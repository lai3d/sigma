apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: sigma-db-pooler-rw
  namespace: sigma
spec:
  cluster:
    name: sigma-db
  instances: 2
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "100"
      default_pool_size: "20"
